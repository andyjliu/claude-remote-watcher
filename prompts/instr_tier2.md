This looks like a trivial crash (bad import/path/name/arg). Diagnose from the log and the script/source. If the fix is small and unambiguous (a typo, a missing import, a wrong path/arg name, a missing directory), apply it in the experiment dir, save the diff to `patches/`, and resubmit **once** by re-running the submit line from the job's workdir (`cd <workdir> && <submit line>`; for a single array task add `--array=<idx>`). Report the new job id. If the fix is not obvious, or touches training logic/hyperparameters/data, do NOT edit anything: describe the likely cause and a proposed patch, and ESCALATE.
